{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Application commands are available only to the trusted main webview.",
  "local": true,
  "windows": ["main"],
  "permissions": ["main-commands"]
}
