{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Application commands and dialog access for the trusted main webview.",
  "local": true,
  "windows": ["main"],
  "permissions": ["main-commands"]
}
